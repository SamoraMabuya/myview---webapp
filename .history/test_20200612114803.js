const express = require('express');
const app = express();
const mysql = require('mysql');


app.use.express();
app.get("/", (req, res) => {
    
})

app.listen( 5500, () => {
    console.log('server has started on this port')
})

